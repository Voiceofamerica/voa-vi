query ProgramVideos($source: Audience!, $zone: Int) {
  content(source: $source, type: [Video], zoneId: $zone) {
    id
    pubDate
    video {
      url
      thumbnailTiny
      videoTitle
      videoDescription
    }
  }
}
