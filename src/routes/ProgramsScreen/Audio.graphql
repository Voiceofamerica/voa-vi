query ProgramAudio($source: Audience!, $category: Int) {
  audioProgram(source: $source, zoneId: $category) {
    id
    date
    zone
    image {
      tiny
      hero
    }
    url
    programTitle
    programDescription
  }
}
